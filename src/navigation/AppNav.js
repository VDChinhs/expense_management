import { NavigationContainer } from "@react-navigation/native";

export default function AppNav() {
    const {userToken} = userContext(AuthContext);
    return(
        <NavigationContainer>

        </NavigationContainer>
    )
}